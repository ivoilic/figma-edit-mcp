{
  "name": "figma-edit-mcp",
  "version": "1.0.0",
  "description": "Figma Edit MCP",
  "scripts": {
    "build": "pnpm build:server && pnpm build:plugin",
    "build:server": "pnpm --filter figma-mcp-server build",
    "build:plugin": "pnpm --filter figma-mcp-plugin build",
    "start:server": "pnpm --filter figma-mcp-server start"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "author": "Ivo Ilić <hello@ivoilic.com>",
  "license": "ISC"
}
